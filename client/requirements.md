## Packages
canvas-confetti | For celebration effects when getting answers right
framer-motion | For playful animations and transitions

## Notes
Speech Recognition API is browser native (window.SpeechRecognition or window.webkitSpeechRecognition)
AudioContext is browser native
Scores API: GET /api/scores, POST /api/scores
